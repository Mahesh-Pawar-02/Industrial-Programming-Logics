/*  Problem statement : Create one typescript application which contains one class named as Circle.
    Circle class contains two characteristic ( class Data members ) as Radius, PI.
    Create one Parameterised constructor which accepts one value and assign it to radius. value of 
    PI memeber is set to 3.14.
    In Circle class we have to one mehod (Behaviours) as Area which will return area of circle 
    After designing the class create two objects of that class by providing some hardcoded value.
    call the method Area by using both the objects.   

    1. Addition method will add Number1, Number2 & return result.
    2. Substraction method will subtract Number1, Number2 & return result.
    3. Multiplication method will multiply Number1, Number2 & return result.
    4. Division method will Divide Number1, Number2 & return result.

    After designing yje class create two objects of that class by providing some hardcoded value.
    call the method by using both the objects.
 */